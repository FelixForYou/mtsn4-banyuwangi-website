const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');menu?.addEventListener('click',()=>nav.classList.toggle('open'));window.addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>40));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('show');const n=e.target.querySelector('[data-count]');if(n&&!n.dataset.done){n.dataset.done=1;const end=+n.dataset.count;let v=0;const step=Math.max(1,Math.ceil(end/45));const t=setInterval(()=>{v=Math.min(end,v+step);n.textContent=v.toLocaleString('id-ID');if(v>=end)clearInterval(t)},25)}}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));document.querySelectorAll('[data-lucide]').forEach(el=>{try{lucide.createIcons()}catch(e){}});
